/*    Challenge 4 
eng

Update and save the ﬁle again, this time as
challenge4.html
In JavaScript, prompt() can be used like this:
var name = prompt("what's your name?", "Bob");
When the page loads a prompt will appear and the
user can add some text. "Bob" is the default text. If you
don't want default text, you can leave the second set of
quotes empty.
Write script that prompts the user for their name, and
then display a sentence in the console that greets the
user by name and welcomes them.

--------------------------------------------------

Actualice y guarde el archivo nuevamente, esta vez como
desafío4.html
En JavaScript, prompt() se puede usar así:
var nombre = prompt("¿cuál es tu nombre?", "Bob");
Cuando se cargue la página, aparecerá un aviso y el
el usuario puede agregar algo de texto. "Bob" es el texto predeterminado. Si usted
no desea texto predeterminado, puede dejar el segundo conjunto de
comillas vacías.
Escriba un script que solicite al usuario su nombre y
luego mostrar una oración en la consola que saluda al
usuario por su nombre y les da la bienvenida.
*/
