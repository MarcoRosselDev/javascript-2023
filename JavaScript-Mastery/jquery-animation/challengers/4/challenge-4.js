/*      Challenge 4
Use jQuery to change the font size to 24 pixels
and the color to red for the paragraphs inside the
div, but not the one outside the div.
*/

(function () {
    $('#main p').css({'color':'red','font-size':'24px'})
})()