(function () {
    'use strict';

    const li = document.querySelectorAll('.lii');
    console.log(li);

    li.forEach(e => e.addEventListener('click', function (e) {
        e.preventDefault
        
    }))
})()