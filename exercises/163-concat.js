// ejercicio 163: Agregar un arreglo al final de otro arreglo.

const numero1 = [0, 1, 2, 3, 4];
const nuemro2 = [5, 6, 7, 8, 9];

const concatenacion = numero1.concat(nuemro2);

console.log(concatenacion);

// al arreglo al que se le aplica .concat tiene la primera posicion en orden
