// Ejercicio 22: Diferencia entre null y undefined.

let a;
let b = null;

console.log(typeof a === "undefined");

// entonces null si vien no tiene valor, deja de ser undefined
// por lo tanto apunta a un espacio en memoria pero este esta vacio
// undefined no apunta a un espacio en memoria
