/* HTTP Patch vs PUT
eng

You may encounter PATCH methods from time to time. 
While it is not nearly as common as the other methods, like PUT, 
it's important to know about it and what it does. 
The PATCH method is intended to partially modify a resource.

Long story short, PATCH isn't nearly as popular as PUT, 
and many servers, even if they allow partial updates, 
will still just use the PUT method for that.
----------------------------------------------------
spa

Es posible que encuentre metodos PATCH de vez en cuando.
Si bien no es tan comun como los otros metodos, como PUT,
es importante conocerlo y saber lo que hace.
El metodo PATCH esta destinado a modificar parcialmente un recurso.

Para resumir, PATCH no es tan popular como PUT,
y muchos servidores, incluso si permiten actualizaciones,
seguiran usando el metodo PUT para eso.
*/