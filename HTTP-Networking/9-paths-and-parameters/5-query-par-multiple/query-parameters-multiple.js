/* Multiple Query Parameters
eng
We mentioned that query parameters are key/value pairs,
that means we can have multiple pairs!

http://example.com?firstName=lane&lastName=wagner

In the example above:

firstName = lane
lastName = wagner
The ? separates the query parameters from the rest of the URL. 
The & is then used to separate every pair of query parameters after that. 
-----------------------------------------------------------
spa
Mencionamos que los parametros de consulta son pares clave/valor,
eso significa que podemos tener varios pares !

http://example.com?firstName=lane&lastName=wagner

En el ejemplo anterior:
nombre = carril
apellido = wagner
El ? separa los parámetros de consulta del resto de la URL.
El & se usa para separar cada par de parámetros de consulta después de eso.
*/