/* XML syntax
eng

XML is a markup language like HTML, 
but it's more generalized in that it does not use predefined tags. 
Just like how in JSON an objects' keys can be called anything, 
XML tags can also have any name.
----------------------------------------------------------------
spa

XML es un lenguaje de marcas como HTML,
pero es mas generalizado porque no utiliza etiquetas predefinidas.
Al igual que en JSON, las claves de los objetos se pueden llamar de cualquier manera,
XML tambien puede tener cualquier nombre.

*/

// XML Example
<root>
    <id>1</id>
    <genre>Action</genre>
    <title>Iron Man</title>
    <director>Jon Favreau</director>
</root>

// The same data but in JSON form:
{
    "id": "1",
    "genre": "Action",
    "title": "Iron Man",
    "director": "Jon Favreau"
}