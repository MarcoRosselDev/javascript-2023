/* Sending JSON

JSON isn't just something we get from the server, 
we can also send JSON data!

In JavaScript, two of the main methods we have access to are 
JSON.parse(), and JSON.stringify().

---------------------------------------------------
spa

JSON no es solo algo que obtenemos del servidor,
tabien podemos enviar datos JSON !

En javaScript, dos de los principales metodos a los que tenemos acceso son
JSON.parse() y JSON.stringify()

*/

/* JSON.stringify()
eng
JSON.stringify() is particularly useful for sending JSON.

As you may expect the JSON stringify() method does the opposite of parse. 
It takes a JavaScript object or value as input and converts it into a string. 
This is useful when we need to serialize the objects into strings to 
send them to our server or store them in a database.
-----------------------------------------------------
spa

JSON.stringify() es particularmete util para enviar JSON.

Como es de esperar, el metodo JSON stringify() hace lo contrario al parse.
Esto es util cuando necesitamos serializar los objetos en cadenas 
para enviarlos a nuestro servidor o almacenarlos en una base de datos.
*/