/* async/await makes error hanling easier
eng

try and catch are the standard way to handle errors in JavaScript, 
the trouble is, the original Promise API with .then didn't allow us 
to make use of try and catch blocks.

Luckily, the async and await keywords do allow it, 
yet another reason to prefer the newer syntax.


--------------------------------------------------------------------
spa

try y catch son la forma estandar de manejar errores en javaScript, 
el problema es que la API original de Promise con .then no no permite 
hacer uso de los bloques try y catch

Afortunadamente, las palabras clave async y await lo permite,
otra razon mas para preferir la sintaxis mas nueva.
*/